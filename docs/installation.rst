============
Installation
============

At the command line::

    $ easy_install django-app-tracker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-app-tracker
    $ pip install django-app-tracker
